apiVersion: v1
kind: Service
metadata:
  name: voters-api-svc
spec:
  selector:
    app: voters-api
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 8090
      targetPort: 8090
      
  
